{
  "name": "speedsearch",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "client:install": "cd client && bun install",
    "server:dev": "bun run --hot index.ts",
    "client:build": "cd client && bun run build",
    "server:start": "bun run seed.ts && bun run index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "hono": "^4.4.3",
    "redis": "^4.6.14"
  }
}